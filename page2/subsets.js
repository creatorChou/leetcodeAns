/**
*	78. Subsets
*	https://leetcode.com/problems/subsets/description/
*/

/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var subsets = function(nums) {
	let result = [];
	let arr = [];
	let start = 0;
	return result;
};

console.log(subsets([1,2,3]));
